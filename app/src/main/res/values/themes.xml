<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EverythingIO" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EverythingIO" parent="Base.Theme.EverythingIO" />

    <style name="LoaderDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="elevation">0dp</item>
        <item name="android:elevation">0dp</item>
        <item name="windowMinWidthMajor">0dp</item>
        <item name="android:windowMinWidthMajor">0dp</item>
        <item name="windowMinWidthMinor">0dp</item>
        <item name="android:windowMinWidthMinor">0dp</item>
    </style>

</resources>